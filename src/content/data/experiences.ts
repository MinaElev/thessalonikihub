import type { Place } from "@/lib/types";

/**
 * Experiences (EXPERIENCES pillar).
 *
 * Intentionally empty. Real tours/activities will be added only when verified,
 * ideally from official operators or provided by the site owner.
 */
export const experiences: Place[] = [];
