import type { Place } from "@/lib/types";

/**
 * Bars & nightlife (DRINK pillar).
 *
 * Intentionally empty. Real venues will be added only with verified public
 * information (never invented details) ideally claimed by the owners.
 */
export const bars: Place[] = [];
