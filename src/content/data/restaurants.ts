import type { Place } from "@/lib/types";

/**
 * Restaurants (EAT pillar).
 *
 * Intentionally empty. Real venues will be added only with verified public
 * information (name, area, category, official website) — never invented phone
 * numbers, prices or hours — ideally claimed by the owners.
 */
export const restaurants: Place[] = [];
